import { addBlock, boardReset, removeBlock, getAnimation } from "./boardAction";
import { playPause, addScore, reset, setAnimation } from "./gameAction";
import { moveDown, moveUp, moveRight, moveLeft, getblock } from "./tetroAction";

export {
  addBlock,
  boardReset,
  removeBlock,
  setAnimation,
  getAnimation,
  playPause,
  addScore,
  reset,
  moveDown,
  moveUp,
  moveRight,
  moveLeft,
  getblock
};
