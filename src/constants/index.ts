import { addBlock, removeBlock, boardReset, getAnimation } from "./boardTypes";
import {
  setAnimation,
  addScore,
  addLines,
  playPause,
  gameOver,
  gameReset
} from "./gameTypes";
import { move, rotate, getBlock } from "./tetroTypes";

export {
  addBlock,
  removeBlock,
  boardReset,
  setAnimation,
  getAnimation,
  addScore,
  addLines,
  playPause,
  gameOver,
  gameReset,
  move,
  rotate,
  getBlock
};
